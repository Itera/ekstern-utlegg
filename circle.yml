machine:
  node:
    version: 6

dependencies:
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash
  cache_directories:
    - "~/.yarn-cache"
  override:
    - yarn install

test:
  post:
    - yarn run lint
    - yarn run test:coverage
    - yarn run coverage:reportci
